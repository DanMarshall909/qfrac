# TODO

- [ ] Configure Git to use `.githooks` via `git config core.hooksPath .githooks`.
- [ ] Build a Godot material/shader that renders a fullscreen quad ready for fractal visualization.
- [ ] Establish CUDA interop (DLL/native library) that computes quaternion fractal data consumable by Godot.
- [ ] Stream CUDA results into a Godot texture each frame.
- [ ] Implement parameter morphing across frames for smooth fractal animation.
- [ ] Add user controls (UI or input mappings) for playback and parameter tweaking.
- [ ] Write developer documentation covering build, CUDA dependencies, and morph controls.
