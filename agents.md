# Agent Notes

- Latest work added linting enforcement and git hook setup.
- Proceed by following the task tracked in `next.md`.
- Use `tools/manage_todo.ps1` to track and complete tasks instead of editing the markdown lists manually.
